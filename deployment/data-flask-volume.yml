---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: compose-data-flask-volume-test
  labels:
    app: compose-flask-test
    env: test
    username: astacc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
